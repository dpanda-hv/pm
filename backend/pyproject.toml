[project]
name = "pm-backend"
version = "0.1.0"
description = "FastAPI backend for the PM MVP"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn>=0.35.0",
]

[tool.uv]
dev-dependencies = [
  "httpx2>=2.4.0",
  "pytest>=8.4.0",
]
